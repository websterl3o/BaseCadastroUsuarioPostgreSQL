{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003, // Ensure this matches the Xdebug port
            "pathMappings": {
                "/var/www": "${workspaceFolder}/src"
            }
        }
    ]
}
